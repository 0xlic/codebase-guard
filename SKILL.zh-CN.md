---
name: codebase-guard
description: "在代码库中识别、使用和维护 guard: 注释，用于把用户确认过的代码事实、纠错结论、业务约束和实现意图沉淀到关键代码位置。Use when Codex reads guard comments while exploring code, when a user correction after model misunderstanding should become a trusted code fact, or when existing guard facts conflict with code changes or user requirements."
---

# Codebase Guard

## 核心原则

使用 `guard:` 注释把“代码即文档”的高可信事实写回代码。`guard:` 的可信度高于普通推断，但不能高于用户的最新明确纠正。

只记录会影响后续理解、修改或排障的事实。不要把临时思路、重复代码含义、显而易见的语法说明写成 guard。

本 skill 不规定 agent 必须读取哪些文件、如何查找代码、调用什么工具或按什么顺序探索代码库。它只规定：当 agent 在已读取的代码中遇到 `guard:` 事实时，必须如何理解、使用和维护它。

## 触发时机

在这些场景中主动使用本 skill：

- 读取代码库时发现已有 `guard:` 注释。
- agent 经过分析后给出的代码理解或结论被用户判定有问题，用户给出纠正，并且 agent 已正确理解该纠正。
- agent 判断某个已确认事实很容易被后续误读，适合推荐沉淀为 `guard:`。
- 用户显式调用本 skill，要求把某个事实写入、更新或移除为 `guard:`。
- 当前修改会改变已有 `guard:` 所描述的事实。
- 已读取到的多个 `guard:` 事实之间存在冲突，或与当前需求、代码实现存在冲突。

## Guard 格式

优先使用所在文件的原生单行注释：

```text
// guard: 该接口返回的 status=2 表示人工复核中，不是失败。
# guard: 这里的 tenant_id 允许为空，空值表示全局配置。
-- guard: history 表只用于回放，不参与实时统计。
```

写法要求：

- 使用中文，除非代码上下文已经主要使用英文。
- 统一使用 `guard:` 关键字，不扩展为 `guard(...)` 或其他变体。
- 写成事实陈述，不写“可能”“猜测”“待确认”。
- 尽量贴近被解释的代码分支、字段、查询、协议或异常处理。
- 一条 guard 只表达一个事实。
- 避免记录敏感信息、密钥、个人隐私或未授权业务数据。

## 工作流

### 1. 读取到 Guard

当已读取的代码中出现 `guard:` 时，把它作为高可信事实参与后续逻辑思考。不要把 guard 当成普通注释忽略，也不要用未验证的代码推断覆盖 guard。

如果读到多个 guard，按它们所在的文件、行号和上下文分别理解。若多个 guard 之间互相矛盾，或它们与当前代码、需求、错误现象矛盾，先向用户说明冲突并确认，不要自行选择一个事实为准。

### 2. 新增

当 agent 经过分析后给出的代码理解或结论被用户判定有问题，用户给出纠正，并且 agent 已正确理解该纠正时，如果该纠正会影响后续 agent 对代码的理解，可以主动建议是否把该事实写成 guard。用户确认后，再把结论写到最关键的位置。

当用户显式要求使用本 skill 写入 guard 时，不需要再次确认“是否写入”，但写入前仍要判断已知上下文中是否存在冲突。若存在冲突，先说明冲突并请用户确认以哪个事实为准；若没有冲突，直接写入。

选择位置只有一个原则：尽量放在后续 agent 查找、扫读和构建上下文时容易读到的位置。不要把 guard 放在很深、很偏、后续上下文大概率不会加载的位置，否则它即使存在也无法参与推理。

如果多个位置都能表达同一事实，优先选择更容易被读到的入口、主流程、聚合文件、接口定义、配置附近或调用链上游位置。

### 3. 更新

当用户最新澄清、当前需求或本次代码修改与旧 guard 冲突时，先说明旧 guard 的内容和冲突点，并询问用户是否更新。用户确认后更新原 guard，不要并排保留过期事实。

当用户显式要求更新 guard 时，仍需检查已知上下文中是否存在其他 guard 或代码事实与新内容冲突。发现冲突时先确认；未发现冲突时直接更新。

### 4. 删除

只有在 guard 已被用户确认失效、或本次改动让它不再成立时才移除。移除前说明原因并询问用户；如果用户要求保留历史语境，改为更新成当前事实。

当用户显式要求移除 guard 时，仍需判断该 guard 是否与当前任务上下文相关，以及移除后是否会让代码缺少关键事实依据。若存在风险，先说明风险并确认；否则直接移除。

## 冲突处理

发现冲突时按顺序处理：

1. 引用具体文件和行号说明相关 guard 的内容。
2. 说明 guard 之间、或 guard 与当前代码/用户要求之间的冲突点。
3. 向用户确认以哪个事实为准。
4. 根据确认结果更新代码或更新 guard。

不要让代码变更与已读到的 guard 长期不一致。若本次任务中读到过相关 guard，完成修改前回看这些 guard，确认没有遗留冲突。

## 维护方式

使用当前 agent 最可靠的代码编辑方式维护 guard，例如补丁编辑、IDE 工具或项目已有脚本。不要为了维护 guard 强制切换到某个固定工具。

若用户明确要求查找 guard，可使用当前环境中最合适的搜索工具。常见情况下可以用 `rg -n "guard:"`，但这不是读取代码前的强制步骤。
